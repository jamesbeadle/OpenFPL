#!/bin/bash

# Set current directory to the directory this script is in
SCRIPT=$(readlink -f "$0")
SCRIPT_DIR=$(dirname "$SCRIPT")
cd $SCRIPT_DIR

PROPOSAL="(
    record {
        title = \"Remove Callback Function\";          
        url = \"https://github.com/jamesbeadle/OpenFPL/blob/master/src/OpenFPL_backend/main.mo\";        
        summary = \"Proposal to remove the endpoint for adding the callback function for creating the DAO controlled neuron.\";
        action = opt variant {
            RemoveGenericNervousSystemFunction = 1000:nat64
        }
    }
)"

./submit_proposal.sh "$PROPOSAL"


PROPOSAL="(
    record {
        title = \"Remove Callback Function\";          
        url = \"https://github.com/jamesbeadle/OpenFPL/blob/master/src/OpenFPL_backend/main.mo\";        
        summary = \"Proposal to remove the endpoint for adding the callback function for creating the DAO controlled neuron.\";
        action = opt variant {
            RemoveGenericNervousSystemFunction = 2000:nat64
        }
    }
)"

./submit_proposal.sh "$PROPOSAL"

PROPOSAL="(
    record {
        title = \"Remove Callback Function\";          
        url = \"https://github.com/jamesbeadle/OpenFPL/blob/master/src/OpenFPL_backend/main.mo\";        
        summary = \"Proposal to remove the endpoint for adding the callback function for creating the DAO controlled neuron.\";
        action = opt variant {
            RemoveGenericNervousSystemFunction = 3000:nat64
        }
    }
)"

./submit_proposal.sh "$PROPOSAL"

PROPOSAL="(
    record {
        title = \"Remove Callback Function\";          
        url = \"https://github.com/jamesbeadle/OpenFPL/blob/master/src/OpenFPL_backend/main.mo\";        
        summary = \"Proposal to remove the endpoint for adding the callback function for creating the DAO controlled neuron.\";
        action = opt variant {
            RemoveGenericNervousSystemFunction = 4000:nat64
        }
    }
)"

./submit_proposal.sh "$PROPOSAL"

PROPOSAL="(
    record {
        title = \"Remove Callback Function\";          
        url = \"https://github.com/jamesbeadle/OpenFPL/blob/master/src/OpenFPL_backend/main.mo\";        
        summary = \"Proposal to remove the endpoint for adding the callback function for creating the DAO controlled neuron.\";
        action = opt variant {
            RemoveGenericNervousSystemFunction = 5000:nat64
        }
    }
)"

./submit_proposal.sh "$PROPOSAL"

PROPOSAL="(
    record {
        title = \"Remove Callback Function\";          
        url = \"https://github.com/jamesbeadle/OpenFPL/blob/master/src/OpenFPL_backend/main.mo\";        
        summary = \"Proposal to remove the endpoint for adding the callback function for creating the DAO controlled neuron.\";
        action = opt variant {
            RemoveGenericNervousSystemFunction = 6000:nat64
        }
    }
)"

./submit_proposal.sh "$PROPOSAL"

PROPOSAL="(
    record {
        title = \"Remove Callback Function\";          
        url = \"https://github.com/jamesbeadle/OpenFPL/blob/master/src/OpenFPL_backend/main.mo\";        
        summary = \"Proposal to remove the endpoint for adding the callback function for creating the DAO controlled neuron.\";
        action = opt variant {
            RemoveGenericNervousSystemFunction = 7000:nat64
        }
    }
)"

./submit_proposal.sh "$PROPOSAL"

PROPOSAL="(
    record {
        title = \"Remove Callback Function\";          
        url = \"https://github.com/jamesbeadle/OpenFPL/blob/master/src/OpenFPL_backend/main.mo\";        
        summary = \"Proposal to remove the endpoint for adding the callback function for creating the DAO controlled neuron.\";
        action = opt variant {
            RemoveGenericNervousSystemFunction = 8000:nat64
        }
    }
)"

./submit_proposal.sh "$PROPOSAL"

PROPOSAL="(
    record {
        title = \"Remove Callback Function\";          
        url = \"https://github.com/jamesbeadle/OpenFPL/blob/master/src/OpenFPL_backend/main.mo\";        
        summary = \"Proposal to remove the endpoint for adding the callback function for creating the DAO controlled neuron.\";
        action = opt variant {
            RemoveGenericNervousSystemFunction = 9000:nat64
        }
    }
)"

./submit_proposal.sh "$PROPOSAL"

PROPOSAL="(
    record {
        title = \"Remove Callback Function\";          
        url = \"https://github.com/jamesbeadle/OpenFPL/blob/master/src/OpenFPL_backend/main.mo\";        
        summary = \"Proposal to remove the endpoint for adding the callback function for creating the DAO controlled neuron.\";
        action = opt variant {
            RemoveGenericNervousSystemFunction = 10000:nat64
        }
    }
)"

./submit_proposal.sh "$PROPOSAL"

PROPOSAL="(
    record {
        title = \"Remove Callback Function\";          
        url = \"https://github.com/jamesbeadle/OpenFPL/blob/master/src/OpenFPL_backend/main.mo\";        
        summary = \"Proposal to remove the endpoint for adding the callback function for creating the DAO controlled neuron.\";
        action = opt variant {
            RemoveGenericNervousSystemFunction = 11000:nat64
        }
    }
)"

./submit_proposal.sh "$PROPOSAL"

PROPOSAL="(
    record {
        title = \"Remove Callback Function\";          
        url = \"https://github.com/jamesbeadle/OpenFPL/blob/master/src/OpenFPL_backend/main.mo\";        
        summary = \"Proposal to remove the endpoint for adding the callback function for creating the DAO controlled neuron.\";
        action = opt variant {
            RemoveGenericNervousSystemFunction = 12000:nat64
        }
    }
)"

./submit_proposal.sh "$PROPOSAL"

PROPOSAL="(
    record {
        title = \"Remove Callback Function\";          
        url = \"https://github.com/jamesbeadle/OpenFPL/blob/master/src/OpenFPL_backend/main.mo\";        
        summary = \"Proposal to remove the endpoint for adding the callback function for creating the DAO controlled neuron.\";
        action = opt variant {
            RemoveGenericNervousSystemFunction = 13000:nat64
        }
    }
)"

./submit_proposal.sh "$PROPOSAL"

PROPOSAL="(
    record {
        title = \"Remove Callback Function\";          
        url = \"https://github.com/jamesbeadle/OpenFPL/blob/master/src/OpenFPL_backend/main.mo\";        
        summary = \"Proposal to remove the endpoint for adding the callback function for creating the DAO controlled neuron.\";
        action = opt variant {
            RemoveGenericNervousSystemFunction = 14000:nat64
        }
    }
)"

./submit_proposal.sh "$PROPOSAL"

PROPOSAL="(
    record {
        title = \"Remove Callback Function\";          
        url = \"https://github.com/jamesbeadle/OpenFPL/blob/master/src/OpenFPL_backend/main.mo\";        
        summary = \"Proposal to remove the endpoint for adding the callback function for creating the DAO controlled neuron.\";
        action = opt variant {
            RemoveGenericNervousSystemFunction = 15000:nat64
        }
    }
)"

./submit_proposal.sh "$PROPOSAL"

PROPOSAL="(
    record {
        title = \"Remove Callback Function\";          
        url = \"https://github.com/jamesbeadle/OpenFPL/blob/master/src/OpenFPL_backend/main.mo\";        
        summary = \"Proposal to remove the endpoint for adding the callback function for creating the DAO controlled neuron.\";
        action = opt variant {
            RemoveGenericNervousSystemFunction = 16000:nat64
        }
    }
)"

./submit_proposal.sh "$PROPOSAL"

PROPOSAL="(
    record {
        title = \"Remove Callback Function\";          
        url = \"https://github.com/jamesbeadle/OpenFPL/blob/master/src/OpenFPL_backend/main.mo\";        
        summary = \"Proposal to remove the endpoint for adding the callback function for creating the DAO controlled neuron.\";
        action = opt variant {
            RemoveGenericNervousSystemFunction = 17000:nat64
        }
    }
)"

./submit_proposal.sh "$PROPOSAL"

PROPOSAL="(
    record {
        title = \"Remove Callback Function\";          
        url = \"https://github.com/jamesbeadle/OpenFPL/blob/master/src/OpenFPL_backend/main.mo\";        
        summary = \"Proposal to remove the endpoint for adding the callback function for creating the DAO controlled neuron.\";
        action = opt variant {
            RemoveGenericNervousSystemFunction = 18000:nat64
        }
    }
)"

./submit_proposal.sh "$PROPOSAL"

PROPOSAL="(
    record {
        title = \"Remove Callback Function\";          
        url = \"https://github.com/jamesbeadle/OpenFPL/blob/master/src/OpenFPL_backend/main.mo\";        
        summary = \"Proposal to remove the endpoint for adding the callback function for creating the DAO controlled neuron.\";
        action = opt variant {
            RemoveGenericNervousSystemFunction = 19000:nat64
        }
    }
)"

./submit_proposal.sh "$PROPOSAL"

PROPOSAL="(
    record {
        title = \"Remove Callback Function\";          
        url = \"https://github.com/jamesbeadle/OpenFPL/blob/master/src/OpenFPL_backend/main.mo\";        
        summary = \"Proposal to remove the endpoint for adding the callback function for creating the DAO controlled neuron.\";
        action = opt variant {
            RemoveGenericNervousSystemFunction = 20000:nat64
        }
    }
)"

./submit_proposal.sh "$PROPOSAL"

